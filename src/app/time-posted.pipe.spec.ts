import { TimePostedPipe } from './time-posted.pipe';

describe('TimePostedPipe', () => {
  it('create an instance', () => {
    const pipe = new TimePostedPipe();
    expect(pipe).toBeTruthy();
  });
});
